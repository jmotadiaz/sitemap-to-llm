<%~ include('./partials/header', it) %>

<div class="max-w-2xl mx-auto space-y-8 animate-fade-in">
    <div class="space-y-2">
        <a href="/" class="text-slate-500 hover:text-white transition-colors text-sm flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" /></svg>
            Back to Dashboard
        </a>
        <h1 class="text-4xl font-bold text-white">Sitemap to Text</h1>
        <p class="text-slate-400 font-light">Extract a clean list of URLs from any sitemap.</p>
    </div>

    <form action="/txt" method="POST" class="space-y-8 relative">
        <div class="absolute inset-0 -m-8 bg-space-900/50 rounded-3xl border border-white/5 -z-10"></div>

        <!-- Input URL/Source -->
        <div class="space-y-3">
            <label for="input" class="block text-sm font-medium text-slate-300">Sitemap URL <span class="text-red-400">*</span></label>
            <div class="relative group">
                <input type="url" name="input" id="input" required placeholder="https://example.com/sitemap.xml"
                    class="w-full bg-space-950 border border-white/10 rounded-xl px-4 py-3 text-white placeholder-slate-600 focus:outline-none focus:border-accent-500 focus:ring-1 focus:ring-accent-500 transition-all group-hover:border-white/20">
                <div class="absolute right-3 top-3 text-slate-600">
                    <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" /></svg>
                </div>
            </div>
            <p class="text-xs text-slate-500">Supports XML sitemaps, JSON indexes, or direct URL lists.</p>
        </div>

        <!-- Filtering Options -->
        <div class="grid md:grid-cols-2 gap-6">
            <div class="space-y-3">
                <label for="include_patterns" class="block text-sm font-medium text-slate-300">Include Patterns</label>
                <input type="text" name="include_patterns" id="include_patterns" placeholder="/blog, /products"
                    class="w-full bg-space-950 border border-white/10 rounded-xl px-4 py-3 text-white placeholder-slate-600 focus:outline-none focus:border-accent-500 focus:ring-1 focus:ring-accent-500 transition-all">
                <p class="text-xs text-slate-500">Comma-separated. Only keep URLs matching these.</p>
            </div>

            <div class="space-y-3">
                <label for="exclude_patterns" class="block text-sm font-medium text-slate-300">Exclude Patterns</label>
                <input type="text" name="exclude_patterns" id="exclude_patterns" placeholder="/admin, /private"
                    class="w-full bg-space-950 border border-white/10 rounded-xl px-4 py-3 text-white placeholder-slate-600 focus:outline-none focus:border-accent-500 focus:ring-1 focus:ring-accent-500 transition-all">
                <p class="text-xs text-slate-500">Comma-separated. Remove URLs matching these.</p>
            </div>
        </div>

        <div class="pt-4">
            <button type="submit" class="w-full bg-gradient-to-r from-accent-600 to-brand-500 hover:from-accent-500 hover:to-brand-400 text-white font-bold py-4 rounded-xl shadow-lg shadow-accent-500/25 transform transition-all duration-200 hover:scale-[1.02] active:scale-[0.98] flex items-center justify-center gap-2">
                <span>Generate Text File</span>
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
            </button>
        </div>
    </form>
</div>

<%~ include('./partials/footer', it) %>
